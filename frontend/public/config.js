// Runtime config for UEM frontend.
// In production this file is regenerated by deploy.sh after the backend ECS task gets a public IP,
// and uploaded to S3 (CloudFront serves it with no caching).
// In local dev, Vite's proxy in vite.config.js handles /api → backend, so leave apiUrl empty.
window.__UEM_CONFIG__ = {
  apiUrl: ""
};
